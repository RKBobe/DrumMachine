{
    "name": "JUCE Dev Container",
    "image": "mcr.microsoft.com/devcontainers/cpp:1-ubuntu-22.04",

    "features": {
        "ghcr.io/devcontainers/features/desktop-lite:1": {}
    },
    
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libasound2-dev libjack-jackd2-dev libgtk-3-dev libcurl4-openssl-dev && git clone https://github.com/juce-framework/JUCE.git /workspaces/JUCE",

    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.cpptools-extension-pack",
                "ms-vscode.cmake-tools"
            ],
            "settings": {
                "cmake.configureArgs": [
                    "-DJUCE_SOURCE_DIR=/workspaces/JUCE"
                ]
            }
        }
    }
}